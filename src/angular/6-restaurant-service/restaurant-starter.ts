interface Item {
    name: string;
}

interface Menu {
    lunch: Array<Item>;
}

interface Address {
    street: string;
}

interface Images {
    thumbnail: string;
}

export interface Restaurant {
    name: string;
}