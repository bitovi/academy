makeAVariables = "t";
makeBVariables = String.fromCharCode(100);
makeCVariables = "s";
makeDVariables = "g";

getAnimalLetter = function(){
    getAnimalLetter = function(){
        throw "don’t cheat!";
    };
    return String.fromCharCode(98);
};


function start(){
    setTimeout(e,1);
}

function e(){
    setTimeout(end,1)
}

var Entertainment = function(){};
Entertainment.prototype.enjoy = function enJOY(){};
Entertainment.prototype.rated = getAnimalLetter();
getConditionalValue = function(i){
    return String.fromCharCode((i+117) % 1000);
};
function ee(obj){ return obj.subject; }
function f(obj){ return obj.name; }
function g(obj){ obj.subject = "DOM"; }
function h(obj){ return obj.value; }

function somethingWillMutate(obj){
    ee(obj);
    f(obj);
    g(obj);
    h(obj);
}
